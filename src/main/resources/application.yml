spring:
  application:
    name: job-application-tracker

  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/job_tracker_db
    username: postgres
    password: fr001009
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080