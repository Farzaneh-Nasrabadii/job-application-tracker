spring:
  application:
    name: job-application-tracker

  datasource:
    url: jdbc:postgresql://localhost:5433/job_tracker_db
    username: job_user
    password: fr001009
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080